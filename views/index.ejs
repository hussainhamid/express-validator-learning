<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %>></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <ul>
      <% if (locals.users) { %> <% users.forEach(user => { %>
      <li>
        ID: <%= user.id %>,
        <br />
        Name: <%= user.firstName %> <%= user.lastName %>,
        <br />
        Email: <%= user.email %>,
        <br />

        <% if (user.age) { %> Age: <%= user.age %>, <br />
        <% } %> <% if (user.bio) { %> Bio: <%= user.bio %> <br />
        <% } %>
        <br />
        <button onclick="location.href='/<%= user.id %>/update'">Update</button>

        <form
          action="/<%= user.id %>/delete"
          method="POST"
          style="display: inline"
        >
          <button
            type="submit"
            onclick="return confirm('Are you sure you wnat to delet this?')"
          >
            Delete
          </button>
        </form>
      </li>
      <br />

      <% }) %> <% } %>
    </ul>

    <a href="/create">create a user</a>
  </body>
</html>
